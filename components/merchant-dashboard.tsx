'use client'

import { useState } from 'react'
import { useRouter } from 'next/navigation'
import { registerMerchant, createMerchantSale } from '@/app/actions/merchant'
import { formatARS } from '@/lib/finance'
import { Button } from '@/components/ui/button'
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'
import { Input } from '@/components/ui/input'
import { Label } from '@/components/ui/label'
import { Badge } from '@/components/ui/badge'
import { toast } from 'sonner'
import { Building2, LogOut, Plus, Store, WalletCards } from 'lucide-react'
import { signOut } from '@/lib/auth-client'

type Props = { user: { name: string }; merchant: any; sales: any[] }
export function MerchantDashboard({ user, merchant, sales }: Props) {
 const router = useRouter(); const [busy,setBusy]=useState(false); const [sale,setSale]=useState({amount:'150000',term:'6',customerName:''}); const [m,setM]=useState({businessName:merchant?.businessName??'',cuit:merchant?.cuit??'',category:merchant?.category??'',province:merchant?.province??'Buenos Aires',city:merchant?.city??'',address:merchant?.address??'',phone:merchant?.phone??''})
 const save=async()=>{setBusy(true);try{await registerMerchant(m);toast.success('Solicitud de comercio enviada');router.refresh()}catch{toast.error('No pudimos guardar los datos')}finally{setBusy(false)}}
 const create=async()=>{setBusy(true);try{const r=await createMerchantSale({amount:Number(sale.amount),term:Number(sale.term),monthlyRate:7.5,customerName:sale.customerName});if(!r.ok)toast.error(r.error);else{toast.success('Venta financiada creada');router.refresh()}}catch{toast.error('No pudimos crear la venta')}finally{setBusy(false)}}
 return <div className="min-h-svh bg-muted/30"><header className="border-b bg-background"><div className="mx-auto flex h-16 max-w-7xl items-center justify-between px-4"><div className="flex items-center gap-3"><WalletCards className="size-6 text-primary"/><span className="text-lg font-bold">Uni<span className="text-primary">Cred</span></span><Badge variant="secondary">Comercios</Badge></div><div className="flex items-center gap-3 text-sm"><span className="hidden text-muted-foreground sm:block">{user.name}</span><Button variant="ghost" size="sm" onClick={()=>signOut({fetchOptions:{onSuccess:()=>router.push('/')}})}><LogOut className="mr-2 size-4"/>Salir</Button></div></div></header><main className="mx-auto max-w-7xl space-y-6 px-4 py-8"><div><p className="text-sm font-medium text-primary">Portal para comercios</p><h1 className="text-3xl font-bold">Vendé en cuotas</h1><p className="mt-1 text-muted-foreground">Financiá tus ventas y hacé crecer tu negocio con UniCred.</p></div>{!merchant||merchant.status!=='active'?<Card className="border-primary/20"><CardHeader><CardTitle className="flex items-center gap-2"><Building2 className="size-5 text-primary"/>Registrá tu comercio</CardTitle><p className="text-sm text-muted-foreground">{merchant?'Tu solicitud está siendo revisada.':'Completá los datos para comenzar a ofrecer cuotas.'}</p></CardHeader><CardContent className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{[['businessName','Razón social / nombre'],['cuit','CUIT'],['category','Rubro'],['province','Provincia'],['city','Ciudad'],['address','Domicilio'],['phone','Teléfono']].map(([k,l])=><div key={k} className="space-y-2"><Label>{l}</Label><Input value={m[k as keyof typeof m]} onChange={e=>setM({...m,[k]:e.target.value})}/></div>)}<div className="flex items-end"><Button onClick={save} disabled={busy}>{merchant?'Actualizar datos':'Enviar solicitud'}<Plus className="ml-2 size-4"/></Button></div></CardContent></Card>:<div className="grid gap-6 lg:grid-cols-[1fr_1.2fr]"><Card><CardHeader><CardTitle className="flex items-center gap-2"><Plus className="size-5 text-primary"/>Nueva venta financiada</CardTitle><p className="text-sm text-muted-foreground">Cargá una venta y calculá las cuotas para tu cliente.</p></CardHeader><CardContent className="space-y-4"><div className="space-y-2"><Label>Nombre del cliente</Label><Input value={sale.customerName} onChange={e=>setSale({...sale,customerName:e.target.value})}/></div><div className="grid gap-4 sm:grid-cols-2"><div className="space-y-2"><Label>Monto de venta</Label><Input type="number" value={sale.amount} onChange={e=>setSale({...sale,amount:e.target.value})}/></div><div className="space-y-2"><Label>Cuotas</Label><Input type="number" min="1" max="24" value={sale.term} onChange={e=>setSale({...sale,term:e.target.value})}/></div></div><Button className="w-full" onClick={create} disabled={busy||!sale.customerName}>Crear venta y financiar</Button></CardContent></Card><Card><CardHeader><CardTitle>Ventas recientes</CardTitle></CardHeader><CardContent className="space-y-3">{sales.length===0?<p className="py-8 text-center text-sm text-muted-foreground">Aún no registraste ventas.</p>:sales.slice(0,8).map(s=><div key={s.id} className="flex items-center justify-between border-b pb-3 last:border-0"><div><p className="text-sm font-medium">{s.purpose?.replace('Venta en cuotas — ','')}</p><p className="text-xs text-muted-foreground">{s.term} cuotas · {s.status}</p></div><p className="font-mono font-semibold">{formatARS(Number(s.principal))}</p></div>)}</CardContent></Card></div>}</main></div>
}
